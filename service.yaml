apiVersion: v1
kind: Service
metadata:
  name: shipping-service-svc-30
  namespace: atulvermans
  labels:
    app: shipping-service-30
spec:
  type: NodePort
  ports:
    - name: app-port
      targetPort: app-port
      port: 3000
  selector:
    app: shipping-service-30
